name: flutter_mvvm
description: An example of MVVM in Flutter

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  logging: ^1.0.0
  cupertino_icons: ^1.0.2
  rxdart: ^0.27.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  test: ^1.15.7
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
